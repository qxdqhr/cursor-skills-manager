---
description: 每完成一个开发子任务（P0/P1/M 阶段任务）后必须本地 git 提交
alwaysApply: true
---

# 子任务完成后本地 Git 提交

在本项目（`cursor-skills-manager`）中开发时：

1. **粒度**：每完成 [doc/05-开发子任务拆分清单.md](../doc/05-开发子任务拆分清单.md) 中的一条子任务（如 P1-03、M1 整阶段）或一个可独立验收的小步骤后，立即提交到**本地** Git。
2. **不 push**：除非用户明确要求，否则只 `git commit`，不 `git push`。
3. **提交信息**：使用约定式前缀，并标明任务 ID，例如：
   - `feat(M1/P1-03): 实现 scanPersonalSkills`
   - `test(M1/P1-09): core 单元测试`
   - `chore(rules): 子任务完成后本地提交约定`
4. **提交前**：确保 `pnpm typecheck`（或该包相关检查）通过；更新 [CHANGELOG.md](../CHANGELOG.md) 的 `[Unreleased]`。
5. **禁止**：`git config` 修改、`--no-verify`、空提交、把 `node_modules` / `.env` 提交进库。

完成子任务的标准流程：**实现 → 验证 → CHANGELOG → `git add` → `git commit`**，然后再开始下一子任务。
