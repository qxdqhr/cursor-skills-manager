# Cursor Skills Manager — 开发进度

| 项目 | 内容 |
|------|------|
| 文档版本 | v0.1 |
| 更新日期 | 2026-05-29 |
| 当前里程碑 | **v0.3 多 Agent 统一 ✅（M7–M9）** |

---

## 1. 总览

| 里程碑 | 状态 | Git 提交 | 说明 |
|--------|------|----------|------|
| 文档与评审 | ✅ 完成 | `618f6ce` | 需求、选型、接口草案、任务拆分 |
| **M0** 工程骨架 | ✅ 完成 | `51f2c93` | pnpm monorepo、Hono API、Vite Web |
| 规则 | ✅ 完成 | `0340385` | 子任务完成后本地 git 提交 |
| **M1** core 扫描/校验 | ✅ 完成 | `88dfd23` | 扫描、解析、校验、项目只读、`GET /skills` |
| **M2** API + SQLite | ✅ 完成 | `ffac603`…`45d08f3` | 5 个提交，见 §9 |
| **M3** Web 列表/搜索 | ✅ 完成 | （见 §10） | API client、树/列表/搜索、设置页 |
| **M4** 双栏编辑 | ✅ 完成 | （见 §11） | 写入 API + 双栏编辑 UI |
| **M5** Git + 脚本 | ✅ 完成 | （见 §12） | Git 面板、sync-agents、agents-links |
| **M6** i18n + 验收 | ✅ 完成 | （见 §13） | zh/en、主题、smoke、ACCEPTANCE |
| **M7** 多 Agent Registry | ✅ 完成 | `df2de69` | Platform Registry + Binding 只读检查 |
| **M8** Publish/Repair | ✅ 完成 | `73e7885` | publish/repair API + Web 发布 UI |
| **M9** CLI 探测 + 验收 | ✅ 完成 | （本提交） | probe、设置页 CLI、ACCEPTANCE-v0.3 |

```text
[====M0====]…[====M6====] MVP v0.1 ✅  →  [====M7–M9====] v0.3 ✅
```

---

## 2. 已交付能力（按模块）

### 2.1 `packages/core`

| 能力 | 文件/入口 | 里程碑 |
|------|-----------|--------|
| 路径工具 `expandHome` / `assertInsideRoot` | `paths.ts` | M1 |
| Skill 扫描（个人主库） | `scan.ts` → `scanPersonalSkills` | M1 |
| Skill 扫描（项目只读） | `projectScan.ts` | M1 |
| YAML 解析 / 校验 | `parse.ts`, `validate.ts` | M1 |
| skillId / 物理分类 | `skillId.ts` | M1 |
| agents symlink 检查 | `agentsLink.ts` | M1 |
| **平台注册表 / binding 检查** | `platforms/registry.ts`, `checkBinding.ts` | M7 |
| **symlink 发布 / 修复** | `platforms/publish.ts` | M8 |
| **CLI 探测** | `platforms/probe.ts` | M9 |
| **配置读写** `.csm/config.json` + token | `config.ts` | M2 |
| **SQLite FTS 索引** | `db/index.ts`, `db/schema.sql` | M2 |
| **Git 脏文件标记** | `gitDirty.ts` | M2 |
| 单元测试 | `*.test.ts`（11 项） | M1/M2 |

### 2.2 `apps/api`（Hono，`127.0.0.1:3847`）

| 方法 | 路径 | 鉴权 | 里程碑 |
|------|------|------|--------|
| GET | `/api/v1/health` | 否 | M0/M2 |
| GET/PATCH | `/api/v1/config` | Bearer | M2 |
| GET | `/api/v1/skills` | Bearer | M1/M2（`?q=` FTS） |
| GET | `/api/v1/skills/tree` | Bearer | M2 |
| GET | `/api/v1/skills/:skillId` | Bearer | M2 |
| GET | `/api/v1/skills/:skillId/files` | Bearer | M4 |
| POST | `/api/v1/skills/validate` | Bearer | M4 |
| POST | `/api/v1/skills` | Bearer | M4 |
| PUT | `/api/v1/skills/:skillId` | Bearer | M4 |
| DELETE | `/api/v1/skills/:skillId` | Bearer | M4 |
| GET | `/api/v1/search?q=` | Bearer | M2 |
| GET | `/api/v1/git/status`、diff、log | Bearer | M5 |
| POST | `/api/v1/git/commit` | Bearer | M5 |
| POST | `/api/v1/integrations/sync-agents` | Bearer | M5 |
| GET | `/api/v1/platforms`、`:id/bindings` | Bearer | M7 |
| POST | `/api/v1/platforms/:id/publish`、`/repair` | Bearer | M8 |
| POST | `/api/v1/integrations/sync-platforms` | Bearer | M8 |
| GET | `/api/v1/integrations/agents-links` | Bearer | M5 |
| POST | `/api/v1/open` | Bearer | M5 |
| POST | `/api/v1/index/rebuild` | Bearer | M2 |

- 启动时：加载/创建 `.csm/config.json`，校验 SQLite 索引，不健康则自动 `indexRebuild`
- 统一响应：`{ ok, data }` / `{ ok, error: { code, message } }`

### 2.3 `apps/web`

| 能力 | 状态 |
|------|------|
| Vite + React + Tailwind 壳 | ✅ M0 |
| 调用 `/health` 展示主库状态 | ✅ M0 |
| API client + Bearer（localStorage） | ✅ M3 |
| 技能列表 / 分类树 / 防抖搜索 | ✅ M3 |
| 来源筛选、只读徽章、校验状态 | ✅ M3 |
| 设置页（Token + 只读 config） | ✅ M3 |
| 双栏编辑（表单 + CodeMirror + 预览） | ✅ M4 |
| 新建 / 删除 skill | ✅ M4 |
| 附属文件树只读 Tab | ✅ M4 |
| Git 侧栏（status/diff/commit/log） | ✅ M5 |
| 同步 agents 脚本 + Modal | ✅ M5（M8 扩展为「发布到平台」） |
| 平台 binding badges / 筛选 | ✅ M7 |
| 平台 publish / repair | ✅ M8 |
| 列表「未提交」筛选与徽章 | ✅ M5 |
| 打开目录 / 编辑器（POST /open） | ✅ M5 |

---

## 3. 验收与自测命令

```bash
cd /home/qhr/project/cursor-skills-manager
pnpm install
pnpm typecheck
pnpm test                 # core 单测
pnpm verify:m1            # 扫描 ≥17 personal
pnpm dev:api              # 或 pnpm dev

# M2（需 API 已启动；token 见 ~/.cursor/skills/.csm/config.json）
pnpm verify:m2
pnpm verify:m4            # 需 API 已启动（写入会创建并删除临时 skill）
pnpm verify:m5            # 需 API + 主库为 git 仓库
pnpm verify:m7            # 需 API 已启动
pnpm verify:m9            # 需 API 已启动
pnpm smoke                # typecheck + test + verify（API 可选）
```

**最近一次 M2 自测结果**（2026-05-27）：

| 检查项 | 结果 |
|--------|------|
| `GET /health` → `indexOk: true` | ✅ |
| `GET /skills` → total **18**（17 personal + 1 project） | ✅ |
| `GET /search?q=android` → **7** 条命中 | ✅ |
| `GET /skills/tree` | ✅ |
| `POST /index/rebuild` | ✅ |

---

## 4. 配置与数据文件（主库内）

路径：`~/.cursor/skills/`

| 路径 | 说明 | 进 Git |
|------|------|--------|
| `.csm/config.json` | 端口、locale、**api.token**、扫描路径 | ✅（Q1-C） |
| `.csm/index.sqlite` | FTS5 全文索引 | ✅（Q1-C） |
| `.csm/skills/*.json` | 逻辑标签（计划 v0.2） | 待定 |

首次启动 API 时若不存在 `config.json`，会自动创建并生成 Bearer token。

**调用需鉴权接口示例：**

```bash
TOKEN=$(jq -r '.api.token' ~/.cursor/skills/.csm/config.json)
curl -s -H "Authorization: Bearer $TOKEN" \
  'http://127.0.0.1:3847/api/v1/skills' | jq '.data.total'
```

---

## 5. M2 任务完成情况（对照 05 清单）

| ID | 任务 | 状态 |
|----|------|------|
| P2-01 | `.csm/config.json` 读写 + token | ✅ |
| P2-02 | Bearer 鉴权（除 `/health`） | ✅ |
| P2-03 | SQLite 建表 | ✅ |
| P2-04 | `indexRebuild` | ✅ |
| P2-05 | skills / :id / tree | ✅ |
| P2-06 | 搜索 `?q=` + `/search` | ✅ |
| P2-07 | `POST /index/rebuild` | ✅ |
| P2-08 | 启动时索引校验/重建 | ✅ |
| P2-09 | GET/PATCH `/config` | ✅ |
| P2-10 | 统一错误信封 | ✅ |

---

## 6. 已知问题与备注

1. **部分 SKILL.md 的 YAML**（如 `project-bootstrap-remote-assets`）含未加引号的 `:`，解析会标 `PARSE_ERROR`，扫描仍收录该 skill。
2. **Web Token**：首次使用需在设置页粘贴 `~/.cursor/skills/.csm/config.json` 中的 `api.token`。
3. **`indexUpsert`**：M4 保存/新建后单条更新 FTS；删除时 `indexDelete`。

---

## 9. M2 Git 提交记录（2026-05-27）

| 提交 | 说明 |
|------|------|
| `ffac603` | P2-01 config.json + token |
| `701f94c` | P2-03/04 SQLite FTS + gitDirty |
| `c4345e4` | P2-02/10 Bearer + 错误信封 |
| `3a58829` | P2-05～09 路由与启动索引 |
| `45d08f3` | verify:m2 + lockfile |

---

## 7. 下一步（v0.2 backlog）

见 [05-开发子任务拆分清单.md](./05-开发子任务拆分清单.md) §8（标签、skills-add、复制到主库等）。

---

## 10. M3 任务完成情况（对照 05 清单）

| ID | 任务 | 状态 |
|----|------|------|
| P3-01 | API client + localStorage Token | ✅ |
| P3-02 | AppLayout + 视图切换 | ✅ |
| P3-03 | CategoryTree（`GET /skills/tree`） | ✅ |
| P3-04 | SkillList + 来源筛选 | ✅ |
| P3-05 | SearchBar 防抖（`?q=` / `/search`） | ✅ |
| P3-06 | SkillDetailPanel 只读详情 | ✅ |
| P3-07 | SettingsPage（Token + config） | ✅ |

**未纳入 M3**：`gitDirty` 列表筛选（API 未在 skill 对象返回 dirty）、i18n（M6）。

---

## 11. M4 任务完成情况

| ID | 任务 | 状态 |
|----|------|------|
| P4-01 | `POST /skills/validate` | ✅ |
| P4-02 | PUT / POST / DELETE skills | ✅ |
| P4-03 | 项目 skill 写操作 403 | ✅ |
| P4-04～07 | 编辑器 + 预览 + 保存流 | ✅ |
| P4-08 | 保存后 indexUpsert | ✅ |
| P4-09 | 新建 skill 对话框 | ✅ |
| P4-10 | 删除（硬删 + 确认） | ✅ |
| P4-11 | 附属文件列表 | ✅ |

`pnpm verify:m4`：创建 → 校验 → PUT → 项目 403 → 删除。

---

## 12. M5 任务完成情况

| ID | 任务 | 状态 |
|----|------|------|
| P5-01 | simple-git gitService | ✅ |
| P5-02 | GET/POST git 路由 | ✅ |
| P5-03 | GitPanel UI | ✅ |
| P5-04 | POST sync-agents | ✅ |
| P5-05 | 同步 agents Modal | ✅ |
| P5-06 | agents-links + 列表徽章 | ✅ |
| P5-07 | POST /open | ✅ |

`pnpm verify:m5`：git/status、agents-links、sync-agents、gitDirty 筛选。

---

## 13. M6 任务完成情况

| ID | 任务 | 状态 |
|----|------|------|
| P6-01 | react-i18next zh/en | ✅ |
| P6-02 | 语言切换 + PATCH config | ✅ |
| P6-03 | 浅色/深色/跟随系统 | ✅ |
| P6-04 | API 仅 localhost 绑定 | ✅ |
| P6-05 | `pnpm smoke` | ✅ |
| P6-06 | [ACCEPTANCE-v0.1.md](./ACCEPTANCE-v0.1.md) | ✅ |
| P6-07 | [examples/csm-config.sample.json](./examples/csm-config.sample.json) | ✅ |

---

## 14. M7 任务完成情况

| ID | 任务 | 状态 |
|----|------|------|
| P7-01～P7-10 | Platform Registry + Binding | ✅ `df2de69` |

`pnpm verify:m7`：platforms、bindings、config v2、skills 筛选。

---

## 15. M8 任务进度

| ID | 任务 | 状态 |
|----|------|------|
| P8-01 | `platforms/publish.ts` | ✅ |
| P8-02 | POST publish/repair | ✅ |
| P8-03 | POST sync-platforms | ✅ |
| P8-04 | 顶栏发布 Modal | ✅ |
| P8-05 | 详情页单 skill 发布 | ✅ |
| P8-06 | dry-run + force 冲突 | ✅ |
| P8-07 | agentsLink deprecated 说明 | ✅（类型注释 + 文档） |
| P8-08 | verify:m8 | ✅ |

---

## 16. M9 任务完成情况

| ID | 任务 | 状态 |
|----|------|------|
| P9-01～P9-07 | CLI probe、设置页、验收文档、README | ✅ |

`pnpm verify:m9`：`GET /platforms` 含 `cliInstalled`；OpenCode `alternateRoots`。

---

## 8. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.6 | 2026-05-29 | M9 完成；v0.3 验收文档 |
| v0.5 | 2026-05-29 | M7 完成；M8 publish/repair |
| v0.4 | 2026-05-27 | M5 完成：Git 面板与 agents 同步 |
